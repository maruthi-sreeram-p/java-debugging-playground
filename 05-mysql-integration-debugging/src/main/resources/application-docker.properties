# Profile used when the application itself runs inside the compose network,
# where MySQL is reachable by service name on its internal port.
spring.datasource.url=jdbc:mysql://mysql:3306/hrdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=hruser
spring.datasource.password=hrpw

services:
  mysql:
    image: mysql:8.0
    container_name: debuglab12-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: rootpw
      MYSQL_DATABASE: onboardingdb
      MYSQL_USER: onboarduser
      MYSQL_PASSWORD: onboardpw
    ports:
      - "3307:3306"
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-prootpw"]
      interval: 5s
      timeout: 5s
      retries: 20

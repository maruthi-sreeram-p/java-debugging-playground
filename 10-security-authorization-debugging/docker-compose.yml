services:
  mysql:
    image: mysql:8.0
    container_name: debuglab10-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: rootpw
      MYSQL_DATABASE: tasksdb
      MYSQL_USER: taskuser
      MYSQL_PASSWORD: taskpw
    ports:
      - "3307:3306"
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-prootpw"]
      interval: 5s
      timeout: 5s
      retries: 20
